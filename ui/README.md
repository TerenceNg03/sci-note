# Build UI component

```sh
npm install     #install javascript libraries
npm run build   #build react app
npm run dev     #start react dev server
stack build     #copy-bins --build haskell backend
npm run package #package haskell backend & react app with electron
open ./out      #check the output app
```
